export interface IStyledLinks {
  rotate: string;
}

export interface IStyledContainer {
  justifyContent: 'space-between' | 'center';
  alignItems: 'flex-end' | 'center';
}

export interface IStyleBlueDiv {
  isClick: boolean;
}
